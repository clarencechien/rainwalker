# PATCH 決策書 — 鄰站訊號進 fusion + 兩個負結果收案

> 狀態：**待拍板**（本文為計劃，程式尚未改）
> 日期：2026-07（shadow log 累積約兩週、15,360 筆已對答案）
> 依據：`/stats?weeks=2` 實測；原則見 `SHADOWLOG_SPEC.md` §8（human-on-the-loop，人工依數據裁，不自動改門檻）
> 觸發：使用者質疑「資料夠、分數夠高就該 fix，不用死綁四週」——本文用信度效度回應此判斷。

---

## 0. 一句話決策
資料量已達統計定案級（n=13k–15k），**四週死規矩改為「樣本足即可裁」**。三件事定案：
1. **升**：鄰站領先訊號進 fusion gating（14:05 漏報案的正解）。
2. **砍**：Open-Meteo 進 fusion（比 QPF 差且吵，數據判出局）。
3. **砍**：QPF 放寬半徑（淨損，實驗負結果收案）。

---

## 1. 資料基礎（信度前提）
- 樣本：1h 已對答案 **15,360 筆**、鄰站/QPF 半徑實驗 **13,181 筆**（乾站樣本）。
- 覆蓋：expected 15,368 / settled 15,360（cron 幾乎無漏輪）。
- 抽樣點：8 固定校準點（A/B/C + S1–S5），與使用者自訂盤分離（自訂點不入統計）。
- 口徑：下雨 = 實測 p1h ≥ 0.2 mm；「喊雨」= tier ≥ 2。

n 上萬 → 各項 95% 信賴區間半寬 < 0.03，量級足以裁決；下方每項附 CI/z 值。

---

## 2. 逐項裁決（含信度效度與資料證明）

### 2.1 Phase A 核心修復 → 已驗證，收工
| 指標 | W27（修前） | 現在（n=15,360） |
|---|---|---|
| 方向命中 direction_hit | 0.45 | **0.91** |
| 誤報 false_alarm | **0.99** | **0.49** |
| 漏報 miss | 0 | 0.02 |
| 可能性「高」實際下雨率 | **0.02** | **0.46** |

- **效度**：修的是「打分視野錯配 + 可能性放水」，指標對應改善——誤報腰斬、「高」從喊心酸(2%)變真有一半下(46%)、漏報仍守在 0.02（Type-I 未破功）。
- **信度**：n=15,360，量級碾壓，無需假設檢定。
- 結論：**不需要等待，已 fixed。**

### 2.2 鄰站領先訊號 → **升級進 fusion**
資料（乾站時，鄰站 10km 內最大 10 分雨強對 1h 後本站下雨）：
```
base_rate = 0.05   （乾站當下，1h 內自然下雨率）
th≥2mm/h：precision 0.29  95%CI [0.267, 0.313]   recall 0.69  CI [0.655, 0.725]   n=1,508
th≥5mm/h：precision 0.44  95%CI [0.399, 0.481]                                    n=563
```
- **效度（construct）**：機制是移入型降雨——雨胞在上風、往你移動，鄰站先濕。物理成立，非偽相關。
- **效度（predictive）**：th≥2 的 recall 0.69＝接得住七成「現在乾、等下下」的案例；這正是 14:05 漏報要補的召回。
- **信度**：th≥2 precision 下界 0.267 仍為 base_rate（0.05）的 **5.3 倍**，CI 完全不觸及雜訊區；th≥5 lift 更達 8–9 倍。**訊號真實且穩定。**
- **校準對位**：th≥5 的 precision 0.44 ≈ 現行「可能性高」的實際下雨率 0.46 → 對得上「高」；th≥2 的 0.29 介於現行「中」與「高」之間 → 對「中」。
- 結論：**資料充分支持升級，這一針現在就能下。**

### 2.3 Open-Meteo best_match → **不進 fusion（砍）**
```
CWA-QPF ：accuracy 0.86  false_alarm 0.49  miss 0.03
OM-best ：accuracy 0.75  false_alarm 0.66  miss 0.04    （n=15,360）
差異：accuracy 0.110，z = 24.6（極顯著）
```
- **效度**：OM 全球混合模型對雙北午後對流的 1h 命中確實較差，且誤報高（喊雨 66% 空報）。
- **信度**：z=24.6，遠超顯著門檻，非偶然。
- **重要更正**：7/6「OM 87% 救場」是**倖存者偏誤**——那天對，但整體 OM 過度預報。把它當降雨觸發器 = 把 W27 的病請回來。
- 附帶：藍色「◇ 外部參考」提示線（om_pop≥70）也連帶檢討——它大概率在許多沒下的午後亂喊，本 patch 一併**下架該提示線**（保留 shadow 影子欄位繼續記錄，但不再上畫面）。
- 結論：**OM 出局；JMA（accuracy 0.85 ≈ QPF）留作 CWA 掛掉時備胎，不進 fusion。**

### 2.4 QPF 放寬半徑 → **不放寬（砍）**
```
窄 1.5 格：accuracy 0.89  false_alarm 0.74  miss 0.02
寬 4.5 格：accuracy 0.82  false_alarm 0.80  miss 0.01    （n=13,181）
差異：accuracy 0.070，z = 16.2（極顯著，放寬淨損）
```
- 放寬只換到 miss 少 0.01，卻讓準確度掉 7 個百分點、誤報升到 0.80。**淨損。**
- 結論：**維持窄半徑；nearbyHint 內的 qpf_w 後備門檻一併移除（避免它獨立亂喊）。** 寬 QPF 實驗負結果收案。

---

## 3. 要改什麼（程式，待拍板後執行）

### 3.1 `buildNowcast`：鄰站進判語（核心變更）
- 新增選用參數 `nb`（10km 內鄰站最大 10 分雨強）傳入。
- 在「乾站、無 QPF」分支中，鄰站達標時**由判語背書**，取代原本純提示層：

| 條件（乾站、QPF=0） | tier | 可能性 | claim | 主判語 |
|---|---|---|---|---|
| nb ≥ 5 | 2 | 高 | **1h** | 「鄰區在下，可能移入」＋帶傘 |
| nb ≥ 2 | 1 | 中 | **1h** | 「鄰區有雨，留意移入」 |
| 僅 h3 訊號 | 0 | 中/低 | 3h | （現行「這 1 小時應不會下…帶傘保險」） |

- **關鍵**：鄰站是 1h 主張 → `claim="1h"`（進 1h 帳受考），不再是 3h。tier 保守（不知移入強度，故 nb≥5→tier2、nb≥2→tier1），讓 direction_hit 不虛高。

### 3.2 提示層清理
- 移除 `om_hint`（OM 參考行，2.3）與 `nearbyHint` 內的 `qpf_w` 後備分支（2.4）。
- `nb_hint` 提示層被 3.1 的判語取代 → 移除（避免與主判語重複）。
- `om_mm/om_pop/om_jma/qpf_w/nb_r10` **shadow 影子欄位全數保留**（持續對答案，日後可回頭複驗）。

### 3.3 門檻常數
- 於 `GATE` 旁新增 `NB_MID=2, NB_HIGH=5`（人工調參區，註明數據來源與日期）。

### 3.4 文件
- `SHADOWLOG_SPEC.md`：§8 原則加「樣本足（單桶 n≥1000 或關鍵指標 CI 半寬<0.03）即可人工裁，不綁固定週數」；記本次三項裁決。
- `HANDOFF.md`：影子實驗 backlog 更新——鄰站已升、OM/寬QPF 收案。

---

## 4. 風險與取捨（誠實揭露）
- **誤報會升**：鄰站 th≥2 precision 0.29＝喊了有 71% 該點 1h 內沒下。這是 Type-I（最怕漏報）的**刻意取捨**——代價僅「帶把傘」，換 recall 0.69。故 nb≥2 只給 tier1「留意」（不算重度喊雨），nb≥5（precision 0.44）才給 tier2「帶傘」，警度對齊證據強度。
- **週報指標會變**：升級後 `scores_1h.false_alarm` 預期小幅上升、`miss` 下降——這是「把漏報換成可接受的誤報」，符合定位。需在下版週報確認 miss 確實下降、false_alarm 升幅可控（預估 +0.02～0.05）。
- **可回滾**：全部改動集中在 `buildNowcast` 一處分支 + 提示層移除；如週報顯示 miss 沒降或 false_alarm 爆升，`NB_MID/NB_HIGH` 調高或整段還原即可。

---

## 5. 驗收（部署後）
1. `/at` 打一個乾站但鄰站在下的點 → 主判語應為「鄰區在下/有雨」而非「這 1 小時應不會下」。
2. 下一場移入型雨：7/6 永和劇本重演時，14:30（鄰站=15）主判語應直接喊出，不再躺灰字。
3. 下版週報：`scores_1h.miss` 應 ≤ 現行 0.02 且不升；`false_alarm` 升幅 ≤ 0.05。
4. `tests/offline.test.mjs`：新增鄰站升級的合成案例（nb≥5→高/tier2/claim1h、nb≥2→中/tier1、邊界不誤觸），全綠。

---

## 6. 方法論修訂（回應「不用過耦合」）
四週原是**保守預設**，非教條。本次以 n=13k–15k、CI 與 z 值證明：資料足時，**人工依信度效度即可裁決**，仍屬 human-on-the-loop（人看數據拍板，非程式自動調門檻）。此後影子實驗裁決標準：**關鍵指標 95%CI 半寬 < 0.03 且方向明確即可結案**，不綁固定週數。
